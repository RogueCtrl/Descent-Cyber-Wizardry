# Implementation Plan: [Feature Name]

This document details the exact technical implementation steps for adding the [Feature Name] feature.

## 1. Modify `path/to/primary_file.ts`
[Explain the core changes happening in the most important file.]

### Updated Method: `existingMethodName()`
[If modifying existing functions, explain the exact logic changes or signature updates. Provide a code snippet if helpful.]
```typescript
// Example snippet
```

### New Method: `newMethodName()`
[If adding new functions, outline their responsibilities, inputs, and outputs step-by-step.]
1. [Step 1]
2. [Step 2]

## 2. Refinements in `path/to/secondary_file.ts`
[Detail changes needed in supporting files, models, or configurations.]

## 3. Edge Cases & Constraints
[Provide strict instructions on how to handle edge cases, specific APIs to call, or anti-patterns to avoid during implementation. Warn the implementing agent about potential side-effects.]
